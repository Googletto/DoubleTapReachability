name: Build DoubleTapReachability   # You can rename this to "Build DoubleTapReachability"

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Install Theos
        uses: Randomblock1/theos-action@v1

      - name: Build the package
        run: |
          make package FINALPACKAGE=1

      - name: Upload the .deb
        uses: actions/upload-artifact@v4
        with:
          name: DoubleTapReachability-deb
          path: packages/*.deb
